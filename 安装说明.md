# CAD套件：新电脑安装与更新

唯一整套入口：[que-yaodong/solidworks-automation-skill](https://github.com/que-yaodong/solidworks-automation-skill)。
下载仓库默认 `main` 分支的 **Code → Download ZIP** 并解压到 D 盘临时目录，或使用 Git 克隆。这是用户维护的分支；保留上游作者署名和 MIT 许可证。

在解压后的仓库目录打开 PowerShell，运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1
```

默认一次注册 **6 项 Skill**，完整资料保存在 `D:\ChatGPT\CAD套件`。客户端入口使用 `$env:CODEX_HOME/skills`，未设置则使用当前用户的 `.codex/skills`。入口是目录联接，资料本体仍在 D 盘。刷新或重启客户端后检查这些名称：

- `solidworks-automation`
- `autocad-automation`
- `solidworks-engineering-drawing`
- `solidworks-fillet-chamfer-cnc`
- `solidworks-threaded-holes`
- `solidworks-vibecad`

下载目录和安装目录必须不同。没有 D 盘时显式指定另一个资料目录：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -LibraryRoot 'E:\ChatGPT\CAD套件'
```

仅检查下载文件、不安装：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install.ps1 -VerifyOnly
```

保留 `-SkillName 名称` 单项入口；单项模式仍复制完整套件以保留共同依赖，只注册指定技能。可用 `-CodexHome` 指定客户端主目录，或用 `-CodexSkillsRoot` 精确指定 skills 目录，两者不能同时使用。

## 更新与恢复

下载最新版到新的临时目录，再运行相同命令。安装器校验 `install-manifest.json` 中的文件和 Skill 名称，然后复制、再次校验、整体替换资料目录并注册入口。Git 检出的 CRLF 与 ZIP 的 LF 换行均可校验。校验不是数字签名，下载源仍需认准上方仓库。

旧资料完整移动到安装目录同级的 `.suite-backups/`，旧客户端入口放在 `skills/.suite-entry-backups/`。后续注册失败会回退此次已完成的替换；回退受文件占用等影响时会报告备份位置。安装结果和备份路径写入资料目录的 `.suite-installation.json`。

**更新不会自动合并本地修改。** 自己补充的知识、配置或脚本应先提交到维护仓库，或从备份中人工核对迁移。不要把正在维护的 Git 工作区作为安装目标；不要直接在安装目录拉取 Git 更新。

## 运行依赖

安装成功表示 Skill 和关联文件就绪。SolidWorks/AutoCAD、许可证、Python 及对应 CAD 依赖需要按实际任务另行配置。安装器不执行 pip、不注册 MCP、不启动 CAD；完整父仓库和五个子技能共享同一份 scripts、references。解析子技能路径时使用联接目标的真实路径，再定位父仓库。

## 维护发布

修改源码和文档后先审查变更，再在仓库根目录执行 `python scripts/update-install-manifest.py`，运行 `install.ps1 -VerifyOnly` 并使用隔离目录试装，通过后连同清单一起提交。只更新文件、不更新清单会被安装器拒绝。不要将账号令牌、客户模型或本地生成文件加入 Git。
